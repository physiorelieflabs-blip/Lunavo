import Marketplace from '@/pages/marketplace';

/** Public aggregate storefront. /marketplace remains as a backwards-compatible alias. */
export default function GeneralStore() {
  return <Marketplace />;
}