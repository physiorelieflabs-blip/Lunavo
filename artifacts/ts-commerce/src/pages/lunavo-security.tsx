import { AlertTriangle, KeyRound, ShieldCheck } from "lucide-react";
import { LunavoApplicationShell } from "../components/lunavo-application-shell";
export default function LunavoSecurity(){return <LunavoApplicationShell title="Security & Access" subtitle="Protect merchant and administrator workspaces with explicit session, credential and authorization controls."><section className="lunavo-feature-grid">{["Two-factor authentication","Login alerts","Device management","Session revocation","Suspicious-login detection","Rate limiting","Brute-force protection","Secure account recovery","Scoped API keys","Webhook secrets","Staff authorization","Audit logs"].map(x=><article className="lunavo-feature-card" key={x}><div className="lunavo-metric-icon"><KeyRound size={17}/></div><strong>{x}</strong><span>Security control designed to remain observable and permission-aware.</span></article>)}</section><section className="lunavo-panel lunavo-integrity-panel"><div><ShieldCheck size={22}/><div><span className="lunavo-kicker">SECURITY BOUNDARY</span><h3>Authentication and authorization stay separate from presentation.</h3><p>A polished interface must never imply access to an operation the authenticated principal is not permitted to perform.</p></div></div><AlertTriangle size={21}/></section></LunavoApplicationShell>}
